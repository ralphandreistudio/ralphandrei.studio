# Your live site URL (no trailing slash) — used for Facebook/Messenger link previews and sitemap.
# Set in Vercel: Project → Settings → Environment Variables → add VITE_SITE_URL for Production.
# If omitted on Vercel, the build uses your VERCEL_URL automatically.
VITE_SITE_URL=https://ralphandrei-studio.vercel.app
